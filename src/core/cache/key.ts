export const UserCacheKey = (key: string) => `user:${key}`;
export const GroupCacheKey = (key: string) => `group:${key}`;
export const MessageCacheKey = (key: string) => `message:${key}`;
export const LidMapCacheKey = (key: string) => `lid_map:${key}`;
